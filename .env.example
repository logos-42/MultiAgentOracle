# ===========================================
# Multi-Agent Oracle - 环境变量配置
# ===========================================
#
# 使用方法：
# 1. 复制此文件为 .env
#    cp .env.example .env
# 2. 在 .env 文件中填入您的 API 密钥
# 3. 运行实验（会自动加载 .env 文件）
#
# 注意：.env 文件已被 .gitignore 忽略，不会提交到仓库
# ===========================================

# --------------------------------------------
# DeepSeek API 配置（推荐用于此项目）
# --------------------------------------------
# 获取 API 密钥: https://platform.deepseek.com/
# DeepSeek API 密钥 (必需，用于使用真实 API)
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# DeepSeek 模型选择：
# - deepseek-chat: 通用聊天模型（默认）
# - deepseek-coder: 代码专用模型
# 示例运行命令：
#   cargo run --example zk_fingerprint_experiment -- --use-api --model deepseek-chat

# --------------------------------------------
# OpenAI API 配置（可选）
# --------------------------------------------
# 获取 API 密钥: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your_openai_api_key_here
#
# 示例运行命令：
#   cargo run --example zk_fingerprint_experiment -- --use-api --provider openai --model gpt-4

# --------------------------------------------
# Anthropic (Claude) API 配置（可选）
# --------------------------------------------
# 获取 API 密钥: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
#
# 示例运行命令：
#   cargo run --example zk_fingerprint_experiment -- --use-api --provider anthropic --model claude-3-opus-20240229

# --------------------------------------------
# 高级配置说明
# --------------------------------------------
#
# 1. 使用模拟模式（无需 API 密钥）：
#    cargo run --example zk_fingerprint_experiment
#
# 2. 使用真实 API 模式：
#    cargo run --example zk_fingerprint_experiment -- --use-api
#
# 3. 指定智能体数量和类型：
#    cargo run --example zk_fingerprint_experiment -- --use-api --agents analytical=5 cautious=3 aggressive=2
#
# 4. 多次运行进行统计测试：
#    cargo run --example zk_fingerprint_experiment -- --use-api --runs 10
#
# 5. 禁用回退机制（API 失败时不自动切换到模拟）：
#    cargo run --example zk_fingerprint_experiment -- --use-api --no-fallback
#
# --------------------------------------------
# 环境变量优先级
# --------------------------------------------
# 1. .env 文件（如果存在）
# 2. 手动设置的环境变量（如 set/export 命令）
# 3. 命令行参数（最高优先级）
#
# 例如：.env 中设置 DEEPSEEK_API_KEY=sk-xxx
#      运行时使用 --provider openai
#      结果：会使用 OPENAI_API_KEY（需单独设置）
# ===========================================
